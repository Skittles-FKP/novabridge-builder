name: NovaBridge Android Cloud Build v17 (EMERALD FINAL LOCKED)

on:
  # 🔒 Worker-triggered build (DO NOT CHANGE)
  repository_dispatch:
    types: [novabridge_android_build_v17_next]

  # Manual trigger (safe)
  workflow_dispatch:

jobs:
  android-build:
    runs-on: ubuntu-latest

    env:
      BUILD_ID: ${{ github.event.client_payload.build_id }}
      ZIP_URL: ${{ github.event.client_payload.zip_url }}
      CALLBACK_URL: ${{ github.event.client_payload.callback_url }}

    steps:
      # ------------------------------------------------------------
      # Checkout
      # ------------------------------------------------------------
      - uses: actions/checkout@v4

      # ------------------------------------------------------------
      # Java 17 (AGP 8.x requirement)
      # ------------------------------------------------------------
      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # ------------------------------------------------------------
      # 🔒 Install system dependency: ImageMagick (for icon generation)
      # ------------------------------------------------------------
      - name: 🧰 Install ImageMagick
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick
          convert --version

      # ------------------------------------------------------------
      # 🔒 Gradle (published, AGP-safe)
      # ------------------------------------------------------------
      - name: 🔧 Install Gradle 8.2.1
        run: |
          GRADLE_VERSION=8.2.1
          echo "Installing Gradle ${GRADLE_VERSION}"
          curl -fL https://downloads.gradle.org/distributions/gradle-${GRADLE_VERSION}-bin.zip -o gradle.zip
          unzip -q gradle.zip
          echo "$PWD/gradle-${GRADLE_VERSION}/bin" >> $GITHUB_PATH
          gradle --version

      # ------------------------------------------------------------
      # Download Android project ZIP from Worker
      # ------------------------------------------------------------
      - name: 📥 Download Android project ZIP
        run: |
          curl -L "$ZIP_URL" -o project.zip
          unzip -q project.zip -d extracted
          ls -la extracted

      # ------------------------------------------------------------
      # Android SDK
      # ------------------------------------------------------------
      - name: 📦 Setup Android SDK
        uses: android-actions/setup-android@v3

      # ------------------------------------------------------------
      # 🔒 FIX: Generate AAPT-safe adaptive icon foreground
      # ------------------------------------------------------------
      - name: 🎨 Generate ic_launcher_foreground.png (AAPT-safe)
        run: |
          ICON_DIR="extracted/app/src/main/res/mipmap-mdpi"
          mkdir -p "$ICON_DIR"

          ICON_PATH="$ICON_DIR/ic_launcher_foreground.png"

          # Create a real, valid PNG (48x48 transparent)
          convert -size 48x48 xc:none "$ICON_PATH"

          echo "Generated foreground icon:"
          file "$ICON_PATH"

      # ------------------------------------------------------------
      # Debug keystore
      # ------------------------------------------------------------
      - name: 🔑 Ensure debug keystore
        run: |
          mkdir -p ~/.android
          keytool -genkeypair \
            -keystore ~/.android/debug.keystore \
            -storepass android \
            -alias androiddebugkey \
            -keypass android \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US" || true

      # ------------------------------------------------------------
      # Build APK (Release)
      # ------------------------------------------------------------
      - name: 🏗️ Build APK (Release)
        working-directory: extracted
        run: |
          gradle assembleRelease --no-daemon

      # ------------------------------------------------------------
      # Locate APK
      # ------------------------------------------------------------
      - name: 🔍 Locate APK
        run: |
          APK_PATH=$(find extracted -type f -name "*.apk" | head -n 1)
          if [ -z "$APK_PATH" ]; then
            echo "❌ APK not found"
            exit 1
          fi
          echo "APK_PATH=$APK_PATH" >> $GITHUB_ENV
          echo "Found APK: $APK_PATH"

      # ------------------------------------------------------------
      # Callback to Worker
      # ------------------------------------------------------------
      - name: ☁️ Notify Worker (callback)
        run: |
          curl -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: ${{ secrets.CALLBACK_TOKEN }}" \
            -d "{
              \"build_id\": \"${BUILD_ID}\",
              \"status\": \"success\",
              \"artifact_apk_url\": \"${APK_PATH}\"
            }"

      # ------------------------------------------------------------
      # Done
      # ------------------------------------------------------------
      - name: ✅ Build completed
        run: |
          echo "Android EMERALD build completed successfully for ${BUILD_ID}"
