name: NovaBridge Android Cloud Build — v16.9.11 NEXT

on:
  workflow_dispatch:
  repository_dispatch:
    types:
      - novabridge_android_build_v16_9_next

jobs:
  android-build-next:
    runs-on: ubuntu-latest

    env:
      BUILD_ID: ${{ github.event.client_payload.build_id }}
      ZIP_URL: ${{ github.event.client_payload.zip_url }}
      CALLBACK_URL: ${{ github.event.client_payload.callback_url }}

      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      R2_BUCKET: ${{ secrets.R2_BUCKET }}

    steps:
      - name: 🧪 Print incoming payload
        run: |
          echo "EVENT: ${{ github.event.action }}"
          echo "BUILD_ID=${BUILD_ID}"
          echo "ZIP_URL=${ZIP_URL}"
          echo "CALLBACK_URL=${CALLBACK_URL}"
          echo '${{ toJson(github.event.client_payload) }}'

      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: 📥 Download Android ZIP
        run: |
          curl -fL "$ZIP_URL" -o android.zip
          unzip -q android.zip -d work
          echo "ZIP tree (top):"
          find work -maxdepth 4 -type f | head -n 200

      - name: 🔍 Detect Android project root
        run: |
          ANDROID_ROOT="$(find work -name gradlew -type f | head -n 1 | xargs dirname)"
          if [ -z "$ANDROID_ROOT" ]; then
            echo "❌ gradlew not found in ZIP"
            exit 1
          fi
          echo "Detected ANDROID_ROOT=$ANDROID_ROOT"
          echo "ANDROID_ROOT=$ANDROID_ROOT" >> "$GITHUB_ENV"

      - name: 🧰 Bootstrap Gradle Wrapper (no ZIP download)
        run: |
          cd "$ANDROID_ROOT"
          mkdir -p gradle/wrapper

          # ✅ FIXED: Maven Central (reliable) wrapper JAR URL
          curl -fL "https://repo1.maven.org/maven2/org/gradle/gradle-wrapper/8.2.2/gradle-wrapper-8.2.2.jar" \
            -o gradle/wrapper/gradle-wrapper.jar

          # Write wrapper properties safely (no heredoc)
          printf '%s\n' 'distributionUrl=https://services.gradle.org/distributions/gradle-8.2.2-bin.zip' \
            > gradle/wrapper/gradle-wrapper.properties

          echo "Wrapper files:"
          ls -la gradle/wrapper

      - name: 🏗️ Build Android APK
        run: |
          cd "$ANDROID_ROOT"
          chmod +x gradlew || true
          ./gradlew assembleRelease || ./gradlew assembleDebug

      - name: 🔍 Locate APK
        run: |
          APK_PATH="$(find "$ANDROID_ROOT" -name "*.apk" | head -n 1)"
          if [ -z "$APK_PATH" ]; then
            echo "❌ No APK found"
            exit 1
          fi
          echo "Found APK at $APK_PATH"
          echo "APK_PATH=$APK_PATH" >> "$GITHUB_ENV"

      - name: ☁️ Upload APK to Cloudflare R2 (public)
        run: |
          OBJECT_KEY="android/${BUILD_ID}.apk"
          PUBLIC_URL="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com/${R2_BUCKET}/${OBJECT_KEY}"

          aws configure set aws_access_key_id "$R2_ACCESS_KEY_ID"
          aws configure set aws_secret_access_key "$R2_SECRET_ACCESS_KEY"
          aws configure set default.region auto

          aws s3 cp "$APK_PATH" "s3://${R2_BUCKET}/${OBJECT_KEY}" \
            --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com" \
            --acl public-read

          echo "APK_PUBLIC_URL=$PUBLIC_URL" >> "$GITHUB_ENV"
          echo "Uploaded APK → $PUBLIC_URL"

      - name: 🔁 Callback NEXT worker (public APK URL)
        run: |
          curl -fS -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"${BUILD_ID}\",\"status\":\"success\",\"platform\":\"android\",\"artifact_url\":\"${APK_PUBLIC_URL}\"}"

      - name: ✅ Build Complete
        run: echo "Android NEXT build completed successfully"
