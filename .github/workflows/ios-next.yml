name: NovaBridge iOS Cloud Build (NEXT)

on:
  repository_dispatch:
    types:
      - novabridge_ios_build_next
  workflow_dispatch:

jobs:
  ios-next-build:
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      # ------------------------------------------------------------
      # 1️⃣ Checkout
      # ------------------------------------------------------------
      - name: Checkout repo
        uses: actions/checkout@v4

      # ------------------------------------------------------------
      # 2️⃣ Extract & normalize dispatch payload
      # ------------------------------------------------------------
      - name: Extract dispatch payload
        shell: bash
        run: |
          set -euo pipefail

          python3 - <<'PY'
          import json, os

          ev = json.load(open(os.environ["GITHUB_EVENT_PATH"], "r"))
          cp = ev.get("client_payload") or {}

          def pick(*keys):
              for k in keys:
                  if cp.get(k):
                      return cp.get(k)
              return ""

          build_id = pick("build_id","buildId")
          zip_url = pick("zip_url","zipUrl")
          callback_url = pick("callback_url","callbackUrl")

          if "/download/ios-zip/" not in zip_url:
              zip_url = zip_url.replace("/download-ios/", "/download/ios-zip/")

          plugins = cp.get("plugins") or cp.get("plugins_json") or {}
          if isinstance(plugins, str):
              try: plugins = json.loads(plugins)
              except: plugins = {}
          if not isinstance(plugins, dict):
              plugins = {}

          with open(os.environ["GITHUB_ENV"], "a") as f:
              f.write(f"BUILD_ID={build_id}\n")
              f.write(f"ZIP_URL={zip_url}\n")
              f.write(f"CALLBACK_URL={callback_url}\n")
              f.write(f"PLUGINS_JSON={json.dumps(plugins, separators=(',', ':'))}\n")
          PY

      # ------------------------------------------------------------
      # 3️⃣ Validate payload
      # ------------------------------------------------------------
      - name: Validate dispatch payload
        run: |
          set -e
          [ -n "$BUILD_ID" ] || exit 1
          [ -n "$ZIP_URL" ] || exit 1
          [ -n "$CALLBACK_URL" ] || exit 1

      # ------------------------------------------------------------
      # 4️⃣ Download iOS ZIP (R2)
      # ------------------------------------------------------------
      - name: Download iOS ZIP
        run: |
          set -e
          rm -rf work
          mkdir -p work
          curl -fL "$ZIP_URL" -o ios.zip
          unzip -t ios.zip
          unzip -q ios.zip -d work

      # ------------------------------------------------------------
      # 5️⃣ Tooling
      # ------------------------------------------------------------
      - name: Install xcodegen
        run: brew install xcodegen

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # ------------------------------------------------------------
      # 6️⃣ Locate project.yml
      # ------------------------------------------------------------
      - name: Locate iOS project directory
        run: |
          set -e
          SPEC=$(find work -type f -name "project.yml" | head -n 1)
          [ -n "$SPEC" ] || exit 1
          echo "IOS_DIR=$(dirname "$SPEC")" >> "$GITHUB_ENV"

      # ------------------------------------------------------------
      # 7️⃣ Apply iOS plugins
      # ------------------------------------------------------------
      - name: Apply iOS plugins
        run: |
          set -e
          cd tools/ios-plugins
          npm ci || npm install
          ./node_modules/.bin/tsc
          cd ../..

          INFO_PLIST=$(find "$IOS_DIR" -name "Info.plist" | head -n 1)
          ENT=$(find "$IOS_DIR" -name "*.entitlements" | head -n 1 || true)

          if [ -z "$ENT" ]; then
            ENT="$(dirname "$INFO_PLIST")/App.entitlements"
            echo '<?xml version="1.0" encoding="UTF-8"?><plist version="1.0"><dict/></plist>' > "$ENT"
          fi

          node tools/ios-plugins/dist/injectIosPlugins.js \
            --infoPlist "$INFO_PLIST" \
            --entitlements "$ENT" \
            --plugins "$PLUGINS_JSON"

      # ------------------------------------------------------------
      # 8️⃣ Generate Xcode project
      # ------------------------------------------------------------
      - name: Generate Xcode project
        run: |
          set -e
          cd "$IOS_DIR"
          xcodegen generate

      # ------------------------------------------------------------
      # 9️⃣ Resolve scheme
      # ------------------------------------------------------------
      - name: Resolve scheme
        run: |
          set -e
          XCODEPROJ=$(find "$IOS_DIR" -name "*.xcodeproj" | head -n 1)
          SCHEME=$(xcodebuild -list -project "$XCODEPROJ" | sed -n '/Schemes:/,$p' | sed -n '2p' | xargs)
          echo "XCODEPROJ=$XCODEPROJ" >> "$GITHUB_ENV"
          echo "SCHEME=$SCHEME" >> "$GITHUB_ENV"

      # ------------------------------------------------------------
      # 🔟 Build with Xcode
      # ------------------------------------------------------------
      - name: Build with Xcode
        run: |
          set -e
          xcodebuild \
            -project "$XCODEPROJ" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            build

      # ------------------------------------------------------------
      # 1️⃣1️⃣ Package ZIP artifact
      # ------------------------------------------------------------
      - name: Package artifact
        run: |
          set -e
          cd "$IOS_DIR"
          zip -qr "$GITHUB_WORKSPACE/ios-build-${BUILD_ID}.zip" .

      # ------------------------------------------------------------
      # 1️⃣2️⃣ Upload artifact to GitHub
      # ------------------------------------------------------------
      - uses: actions/upload-artifact@v4
        with:
          name: ios-next-${{ env.BUILD_ID }}
          path: ios-build-${{ env.BUILD_ID }}.zip

      # ------------------------------------------------------------
      # ✅ 1️⃣3️⃣ UPLOAD ZIP TO iOS NEXT WORKER (R2) — FINAL FIX
      # ------------------------------------------------------------
      - name: Upload ZIP to iOS NEXT Worker (R2)
        run: |
          set -e
          WORKER_BASE="${CALLBACK_URL%/callback-ios}"
          curl -X PUT "$WORKER_BASE/upload-artifact-ios/$BUILD_ID" \
            -H "x-novabridge-token: ${{ secrets.CALLBACK_TOKEN }}" \
            --data-binary "@ios-build-${BUILD_ID}.zip"

      # ------------------------------------------------------------
      # 1️⃣4️⃣ Notify Worker (terminal flip)
      # ------------------------------------------------------------
      - name: Notify Worker
        run: |
          curl -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: ${{ secrets.CALLBACK_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"status\":\"success\",\"terminal\":true}"

      - name: Done
        run: echo "✅ iOS NEXT build completed successfully"
