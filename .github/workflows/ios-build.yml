name: NovaBridge iOS Build (Generated Project)

on:
  repository_dispatch:
    types: [novabridge_ios_build_v1]

jobs:
  ios-build:
    runs-on: macos-latest
    timeout-minutes: 45

    env:
      BUILD_ID: ${{ github.event.client_payload.build_id }}
      ZIP_URL: ${{ github.event.client_payload.zip_url }}
      CALLBACK_URL: ${{ github.event.client_payload.callback_url }}
      CALLBACK_TOKEN: ${{ secrets.CALLBACK_TOKEN }}

      # Optional IPA signing secrets (only used if build_ipa=true later)
      IOS_P12_BASE64: ${{ secrets.IOS_P12_BASE64 }}
      IOS_P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
      IOS_TEAM_ID: ${{ secrets.IOS_TEAM_ID }}

    steps:
      # -------------------------
      # 0️⃣ Validate payload
      # -------------------------
      - name: Validate payload
        run: |
          set -e
          echo "➡ BUILD_ID=$BUILD_ID"
          echo "➡ ZIP_URL=$ZIP_URL"
          echo "➡ CALLBACK_URL=$CALLBACK_URL"

          if [ -z "$BUILD_ID" ] || [ -z "$ZIP_URL" ] || [ -z "$CALLBACK_URL" ]; then
            echo "❌ Missing required payload values"
            exit 1
          fi

      # -------------------------
      # 1️⃣ Download ZIP
      # -------------------------
      - name: Download iOS ZIP
        run: |
          set -e
          curl -L "$ZIP_URL" -o ios.zip
          unzip ios.zip -d work

          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"step\": 3, \"step_state\": \"done\", \"log\": \"iOS: 📥 Project ZIP downloaded\" }"

      # -------------------------
      # 2️⃣ Install xcodegen
      # -------------------------
      - name: Install xcodegen
        run: |
          brew install xcodegen

      # -------------------------
      # 3️⃣ Generate Xcode project
      # -------------------------
      - name: Generate Xcode project
        run: |
          set -e
          cd work/ios/NovaBridgeDemo-iOS
          xcodegen generate

          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"step\": 4, \"step_state\": \"done\", \"log\": \"iOS: 🏗 Xcode project generated\" }"

      # -------------------------
      # 4️⃣ Locate project + scheme
      # -------------------------
      - name: Locate Xcode project and scheme
        run: |
          set -e

          XCODEPROJ=$(find work -name "*.xcodeproj" | head -n 1)
          if [ -z "$XCODEPROJ" ]; then
            echo "❌ No Xcode project found"
            exit 1
          fi

          SCHEME=$(xcodebuild -list -project "$XCODEPROJ" | awk '/Schemes:/ {getline; print $1}')
          if [ -z "$SCHEME" ]; then
            echo "❌ No scheme found"
            exit 1
          fi

          echo "XCODEPROJ=$XCODEPROJ" >> $GITHUB_ENV
          echo "SCHEME=$SCHEME" >> $GITHUB_ENV

          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"step\": 5, \"step_state\": \"done\", \"log\": \"iOS: 📱 Queued on macOS runner (scheme: $SCHEME)\" }"

      # -------------------------
      # 5️⃣ Build with Xcode
      # -------------------------
      - name: Build iOS app
        run: |
          set -e

          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"step\": 6, \"step_state\": \"active\", \"log\": \"iOS: 🔨 Building with Xcode\" }"

          xcodebuild \
            -project "$XCODEPROJ" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination generic/platform=iOS \
            CODE_SIGNING_ALLOWED=NO \
            build

          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"step\": 6, \"step_state\": \"done\", \"log\": \"iOS: ✅ Build completed\" }"

      # -------------------------
      # 6️⃣ Package build artifact (ZIP)
      # -------------------------
      - name: Package iOS artifact
        run: |
          set -e
          cd work
          zip -r ios-artifact.zip .

          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"step\": 7, \"step_state\": \"done\", \"log\": \"iOS: 📦 iOS build packaged\" }"

      # -------------------------
      # 7️⃣ Upload ZIP artifact (AUTHENTICATED)
      # -------------------------
      - name: Upload ZIP artifact to Worker
        run: |
          set -e

          curl -X PUT \
            --data-binary @work/ios-artifact.zip \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            "https://novabridge-ios-build-worker.novabridge.workers.dev/upload-artifact-ios/$BUILD_ID"

      # -------------------------
      # 8️⃣ (Optional) Build & Upload IPA (ADDITIVE – future-safe)
      # -------------------------
      - name: Upload IPA if generated
        if: success()
        run: |
          if [ -f "work/NovaBridge.ipa" ]; then
            echo "📦 IPA detected — uploading"
            curl -X PUT \
              --data-binary @work/NovaBridge.ipa \
              -H "x-novabridge-token: $CALLBACK_TOKEN" \
              "https://novabridge-ios-build-worker.novabridge.workers.dev/upload-artifact-ios/$BUILD_ID?type=ipa"
          else
            echo "ℹ️ No IPA present (ZIP-only build)"
          fi

      # -------------------------
      # 9️⃣ FINAL SUCCESS CALLBACK
      # -------------------------
      - name: Final success callback
        run: |
          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"status\": \"success\", \"log\": \"iOS: 🎉 Build complete\" }"

      # -------------------------
      # ❌ Failure handler
      # -------------------------
      - name: Failure callback
        if: failure()
        run: |
          curl -s -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -d "{ \"build_id\": \"$BUILD_ID\", \"status\": \"failed\", \"log\": \"iOS: ❌ Build failed in GitHub Actions\" }"
