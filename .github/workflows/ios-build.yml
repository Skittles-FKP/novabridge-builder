name: NovaBridge iOS Build

on:
  repository_dispatch

jobs:
  ios-build:
    runs-on: macos-latest

    env:
      BUILD_ID: ${{ github.event.client_payload.build_id }}
      ZIP_URL: ${{ github.event.client_payload.zip_url }}
      CALLBACK_URL: ${{ github.event.client_payload.callback_url }}
      CALLBACK_TOKEN: ${{ secrets.CALLBACK_TOKEN }}

    steps:
      - name: 🔎 Validate payload
        run: |
          set -e
          echo "➡ BUILD_ID=$BUILD_ID"
          echo "➡ ZIP_URL=$ZIP_URL"
          echo "➡ CALLBACK_URL=$CALLBACK_URL"
          echo "➡ CALLBACK_TOKEN_LENGTH=${#CALLBACK_TOKEN}"

          [ -z "$CALLBACK_TOKEN" ] && echo "❌ CALLBACK_TOKEN missing" && exit 1

      - name: 📥 Download iOS ZIP
        run: |
          set -e
          curl -fL "$ZIP_URL" -o ios.zip
          unzip -q ios.zip
          echo "📂 ZIP contents:"
          ls -R ios

          curl -s -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"step\":3,\"step_state\":\"done\",\"log\":\"iOS: 📥 Project downloaded\"}"

      - name: 🧰 Install XcodeGen
        run: |
          set -e
          brew install xcodegen

      - name: 🧩 Generate Xcode project
        run: |
          set -e
          cd ios/NovaBridgeDemo-iOS

          if [ ! -f project.yml ]; then
            echo "❌ project.yml not found"
            exit 1
          fi

          xcodegen generate

          echo "📂 Generated files:"
          ls

          curl -s -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"step\":4,\"step_state\":\"done\",\"log\":\"iOS: 🧩 Xcode project generated\"}"

      - name: 🔨 Build iOS app
        run: |
          set -e
          cd ios/NovaBridgeDemo-iOS

          PROJECT=$(ls *.xcodeproj | head -n 1)
          SCHEME=$(xcodebuild -list -project "$PROJECT" | awk '/Schemes:/ {getline; print $1}')

          echo "🎯 Building $PROJECT ($SCHEME)"

          curl -s -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"step\":6,\"step_state\":\"active\",\"log\":\"iOS: 🔨 Building with Xcode\"}"

          xcodebuild \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            build

          curl -s -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"step\":6,\"step_state\":\"done\",\"log\":\"iOS: ✅ Build completed\"}"

      - name: 📦 Package build
        run: |
          set -e
          APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData -name "*.app" | head -n 1)
          [ -z "$APP_PATH" ] && echo "❌ No .app found" && exit 1

          mkdir artifact
          cp -R "$APP_PATH" artifact/
          zip -r ios-artifact.zip artifact

          curl -s -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"step\":7,\"step_state\":\"done\",\"log\":\"iOS: 📦 Artifact packaged\"}"

      - name: ☁️ Upload artifact to Worker
        run: |
          set -e
          curl -f -X PUT \
            "$CALLBACK_URL?artifact=1&build_id=$BUILD_ID" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            --data-binary "@ios-artifact.zip"

      - name: ✅ Finalize build
        run: |
          set -e
          curl -s -X POST "$CALLBACK_URL" \
            -H "x-novabridge-token: $CALLBACK_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"build_id\":\"$BUILD_ID\",\"status\":\"success\",\"log\":\"iOS: 🎉 Build complete\"}"
